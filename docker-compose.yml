services:
  landing-page:
    build: .
    container_name: landing-page
    ports:
      - "3000:3000"
    expose:
      - "3000"
    restart: unless-stopped
    environment:
      NODE_ENV: production
      PORT: 3000
      HOST: 0.0.0.0
